---
'@crowdstrike/glide-core': patch
---

- Option (formerly Menu Button and Menu Link) now has a `description` attribute.
- Option now allows arbitrary content via a default slot.
- Option now has a maximum width of 350 pixels.
- Option now truncates with an ellipsis and tooltip when it would otherwise overflow.
- Option's border radius has been slightly reduced.
