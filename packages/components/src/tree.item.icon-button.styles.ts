import { css } from 'lit';

export default [
  css`
    .component {
      display: flex;

      --icon-color: var(--icon-button-color);
      --hovered-icon-color: var(--hovered-icon-button-color);
      --size: 1rem;
    }
  `,
];
