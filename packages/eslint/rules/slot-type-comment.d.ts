import { ESLintUtils } from '@typescript-eslint/utils';
export declare const slotTypeComment: ESLintUtils.RuleModule<"addSlotComment" | "addSlotTypeComment" | "addSlotTypeCommentType", [], {
    recommended: boolean;
}, ESLintUtils.RuleListener>;
