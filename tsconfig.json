{
  "allowJs": true,
  "compilerOptions": {
    "declaration": true,
    "experimentalDecorators": true,
    "module": "NodeNext",
    "noImplicitOverride": true,
    "skipLibCheck": true,
    "strict": true,
    "target": "ESNext",
    // So that our import of `package.json` in various files doesn't cause
    // our compiled `./src` files to be nested under `./dist/src`.
    "rootDir": "./src",
    "useDefineForClassFields": false, // https://lit.dev/docs/components/decorators#decorators-typescript
    "verbatimModuleSyntax": true,
    "resolveJsonModule": true,
    "noUncheckedIndexedAccess": true,
    "plugins": [
      {
        "name": "ts-lit-plugin",
        "strict": true,
        "rules": {
          // https://github.com/runem/lit-analyzer/issues/253
          "no-nullable-attribute-binding": "warning",

          // It's unfortunate to disable this. But TS Lit Plugin doesn't provide a way to
          // disable rules for specific files. And our component tests don't import any of the
          // components they reference. Both because the components are imported for tests by
          // `mount()` and because they can't import them given Playwright runs tests in
          // Node.js.
          "no-missing-import": "off"
        }
      }
    ]
  },
  "exclude": ["dist/"]
}
