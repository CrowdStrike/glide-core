# TODO
# The top-level Prettier shouldn't format individual packages, which 
# have their own Prettier configurations. Add `packages/*` and `storybook` 
# and remove `dist/` from this file if this bug is fixed: https://github.com/prettier/prettier-vscode/issues/3003

LICENSE
pnpm-lock.yaml
dist/
